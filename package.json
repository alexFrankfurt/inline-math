{
  "name": "inlinemath",
  "displayName": "Inline Math Overlay",
  "description": "Shows lightweight math overlays for Java code (starting with division).",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:java"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "inlinemath.peekFraction",
        "title": "InlineMath: Peek Fraction"
      },
      {
        "command": "inlinemath.openMathPreview",
        "title": "InlineMath: Open Math Preview"
      },
      {
        "command": "inlinemath.openMathPreviewLatex",
        "title": "InlineMath: Open Math Preview (LaTeX)"
      }
    ],
    "configuration": {
      "title": "InlineMath",
      "properties": {
        "inlinemath.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable InlineMath overlays."
        },
        "inlinemath.division.codeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show a CodeLens above simple divisions like a/b."
        },
        "inlinemath.division.peekDefinition": {
          "type": "boolean",
          "default": true,
          "description": "Enable Peek Definition for divisions (opens a virtual stacked fraction view)."
        },
        "inlinemath.preview.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the KaTeX-based Math Preview webview."
        },
        "inlinemath.division.inlineDecoration": {
          "type": "boolean",
          "default": false,
          "description": "Show an inline (after) decoration for simple divisions like a/b."
        },
        "inlinemath.division.hoverStackedFraction": {
          "type": "boolean",
          "default": true,
          "description": "Show a stacked two-line fraction in hover for simple divisions like a/b."
        },
        "inlinemath.division.inlinePrefix": {
          "type": "string",
          "default": "  ⟂  ",
          "description": "Prefix shown before the inline fraction decoration."
        },
        "inlinemath.expression.codeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens entries for simple expressions using +, -, *, / and grouping with () or {}."
        },
        "inlinemath.expression.inlineDecoration": {
          "type": "boolean",
          "default": false,
          "description": "Show an inline (after) decoration for expressions (e.g., a+b*c)."
        },
        "inlinemath.expression.hoverLatex": {
          "type": "boolean",
          "default": true,
          "description": "Show a hover with a lightweight preview and LaTeX for expressions."
        },
        "inlinemath.expression.inlinePrefix": {
          "type": "string",
          "default": "  ≈  ",
          "description": "Prefix shown before the inline expression decoration."
        },
        "inlinemath.pow.codeLens": {
          "type": "boolean",
          "default": true,
          "description": "Show CodeLens entries for qualified Math.pow(base, exp) calls (rendered as exponentiation)."
        },
        "inlinemath.pow.inlineDecoration": {
          "type": "boolean",
          "default": false,
          "description": "Show an inline (after) decoration for qualified Math.pow(base, exp) calls."
        },
        "inlinemath.pow.hoverLatex": {
          "type": "boolean",
          "default": true,
          "description": "Show a hover with a lightweight preview and LaTeX for qualified Math.pow(base, exp) calls."
        },
        "inlinemath.pow.inlinePrefix": {
          "type": "string",
          "default": "  ≈  ",
          "description": "Prefix shown before the inline Math.pow decoration."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "katex": "^0.16.11"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.5.4"
  }
}
